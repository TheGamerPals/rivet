# Ubuntu OCI Deployment Security

Follow [setup-autopersonal.md](setup-autopersonal.md). That document uses the exact required paths, `rivet` service user, systemd unit, Caddy TLS proxy, UFW exposure, backup commands, and SPKI pin extraction command from `Rivet_Planning_Package/08_UBUNTU_OCI_DEPLOYMENT_SECURITY.txt`.
